<h1>Edit page</h1>

<%= simple_form_for @client, url: "/clients/#{@client.id}", method: 'patch' do |f| %>
  <%= f.input :first_name %>
  <%= f.input :last_name %>
  <%= f.button :submit %>
<% end %>

<p><%= link_to 'Go back', clients_path %></p>
